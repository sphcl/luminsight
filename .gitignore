# Dependências
node_modules

# Build
dist
dist-ssr

# Cobertura de testes
coverage

# Variáveis de ambiente, NUNCA vai para o repositório
.env
.env.*
!.env.example

# Credencial de admin do Firebase (Admin SDK) usada pelo script de seed.
# NUNCA vai para o repositório. Ver README, seção "Seed do Firestore".
firebase-service-account*.json

# Editor
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Sistema operacional
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Cache
.eslintcache
.prettiercache

# Build info incremental do TypeScript (específico da máquina/checkout), não
# deveria ter sido commitado (estava rastreado desde o commit inicial)
*.tsbuildinfo